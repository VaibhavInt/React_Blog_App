import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  font: {
    fontFamily: "Roboto !important",
  },
});

// we can use to style the individual item of ui material components
